-- Drop the table 'employees' in the 'tutorial' schema
DROP TABLE tutorial.employees;

-- Drop the table 'employees_two' in the 'tutorial' schema
DROP TABLE tutorial.employees_two;

-- It's important to note that the DROP TABLE statement permanently removes the specified table and all its data from the database. 
-- Ensure that you have a backup or that you are certain about the removal before executing such statements.